export const ROLES = {
  ADMIN: "admin",
  HR: "hr",
  EMPLOYEE: "employee",
};

export const ROLE_LABELS = {
  [ROLES.ADMIN]: "Admin",
  [ROLES.HR]: "HR",
  [ROLES.EMPLOYEE]: "Employee",
};

export const ALL_ROLES = Object.values(ROLES);
